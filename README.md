# Getting AWS Certified
Slides for my "Getting AWS certified" talk


### General requirements

This repo presentation uses Ruby + middleman for creating the website, please make sure you have ruby and `bundler` installer then run:

```
bundle install
```

After installing the needed gems run this command to start the local server:

```
bundle exec middleman serve
```
